# ImageLoader
# 面向对象的六大原则
1，职责单一原则：一个类，应该仅有一个引起它变化的原因；

2，开闭原则：对象对于扩展是开放的，对于修改是封闭的；

3，里氏替换原则：所有引用基类的地方必须能透明地使用其子类的对象；

4，依赖倒置原则：高层次的模块不依赖于低层次模块的实现细节；

5，接口隔离原则：客户端不应该依赖于它不需要的接口，，类间的依赖关系应该建立在最小的接口上；

6，迪米特原则：一个对象应该对其他对象有最少的了解；
